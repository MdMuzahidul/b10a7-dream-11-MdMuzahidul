import PropTypes from "prop-types";
const BookMarks = ({ playerLists }) => {
  console.log(playerLists);
  return <div></div>;
};
BookMarks.propTypes = {
  playerLists: PropTypes.func,
};
export default BookMarks;
